{{> header}}
<section id="mySongs">
    <div class="background-spotify">
        <div class="song-container">
            <h1>My Songs</h1>
            {{#each songs}}
            {{>songCard}}
            {{/each}}
        </div>
    </div>
</section>
{{> footer}}